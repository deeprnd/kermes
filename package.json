{
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "format": "cargo clippy --fix --allow-dirty --allow-staged && cargo fmt && prettier */*.js \"*/**/*{.js,.ts}\" -w",
    "lint": "cargo clippy && cargo fmt --check && prettier */*.js \"*/**/*{.js,.ts}\" --check"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/spl-token": "^0.4.12"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.10",
    "chai": "^4.3.4",
    "mocha": "^11.1.0",
    "prettier": "^2.6.2",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.4.5"
  }
}
